# الحفظ المحلي والمزامنة

كل تاجر يملك مساحة بيانات منفصلة مرتبطة بهويته السحابية. البيانات القديمة غير المنسوبة لتاجر في almustaqbal-business-data-v1 محفوظة كما هي ولا تنقل تلقائيًا إلى أي حساب. لا تمسح تخزين المتصفح ما دامت عمليات معلقة.

يحتاج أول دخول وتنزيل التطبيق إلى الإنترنت. بعد ذلك يفتح التطبيق من الكاش، وتحفظ كل عملية والطلب المعلق في كتابة محلية واحدة. المزامنة تستعمل معرفًا ثابتًا للطلب وإصدارًا للسجل لمنع التكرار. التعارض بين جهازين يظهر للمراجعة، وتحتفظ النسخ المحلية القديمة بأرشيف داخل نفس حساب التاجر.

صلاحية الجلسة7أيام، وصلاحية الكتابة دون تحقق اتصال حديث24ساعة. انتهاء الاشتراك يمنع الإضافة والتعديل فورًا ويترك العرض. التجميد والحذف يسريان على الخادم فورًا ويصل إشعارهما للجهاز عند الاتصال. لا يمكن إلغاء صلاحية جهاز غير متصل فورًا.

لتجنب سباق التخزين المتزامن يسمح بتبويبة واحدة للكتابة للحساب على نفس المتصفح؛ البقية تعرض البيانات حتى إغلاق التبويبة الكاتبة. البيانات تحفظ محليًا وسحابيًا بعد تأكيد المزامنة. حدود الطلب100سجل/120KB؛ الطلب الزائد يرفض قبل الحفظ دون حذف بيانات.

Service Worker يحفظ ملفات الواجهة والخطوط فقط. لا يخزن ردود الدخول أو Convex أو version.json. تفعيل التحديث لا يمس بيانات التاجر أو طابور العمليات.

متغيرات البيئة: VITE_CONVEX_URL وVITE_SENTRY_DSN. خادم الإنتاج useful-monitor-231؛ بيانات الاختبارات فقط في rare-wolf-469. Sentry project: almustaqbal-merchant.
