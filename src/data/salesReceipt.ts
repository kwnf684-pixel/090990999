import type {InventorySale} from './localStore';
const escape=(v:unknown)=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
export function salesReceipt(sale:InventorySale){return `<!doctype html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>وصل بيع</title><style>body{font:16px Tahoma,Arial,sans-serif;padding:24px;color:#111}table{width:100%;border-collapse:collapse}td,th{padding:10px;border:1px solid #999;text-align:right}bdi{direction:ltr;unicode-bidi:isolate}@page{margin:15mm}</style></head><body><h1>أعمال المستقبل</h1><h2>وصل بيع</h2><p>رقم العملية: <bdi>${escape(sale.id)}</bdi></p><p>الزبون: ${escape(sale.customerName)}</p><p>التاريخ: <bdi>${escape(sale.date.replace('T',' '))}</bdi></p><table><thead><tr><th>المادة</th><th>الكمية</th><th>سعر البيع</th><th>المجموع</th></tr></thead><tbody>${sale.lines.map(l=>`<tr><td>${escape(l.name)}</td><td><bdi>${escape(l.quantity)}</bdi></td><td><bdi>${escape(l.salePrice)}</bdi></td><td><bdi>${escape(Number((l.quantity*l.salePrice).toFixed(6)))}</bdi></td></tr>`).join('')}</tbody></table><h3>الإجمالي: <bdi>${escape(sale.total)} ${escape(sale.currency)}</bdi></h3></body></html>`;}
export function printSale(sale:InventorySale){const frame=document.createElement('iframe');frame.title='وصل بيع للطباعة';frame.style.cssText='position:fixed;width:1px;height:1px;left:-10000px;border:0';frame.onload=async()=>{const win=frame.contentWindow;if(!win)return;await win.document.fonts.ready;win.addEventListener('afterprint',()=>frame.remove(),{once:true});win.focus();win.print();setTimeout(()=>frame.remove(),60000);};frame.srcdoc=salesReceipt(sale);document.body.appendChild(frame);}
