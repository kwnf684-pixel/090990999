import {useState} from 'react';import {hideable,useHiddenMenus,toggleMenu} from '../data/menuVisibility';
export default function MenuSettings(){const hidden=useHiddenMenus(),[open,setOpen]=useState(false),[error,setError]=useState('');return <section className="panel"><h2>القوائم الظاهرة</h2><button type="button" className="tl-button" aria-expanded={open} onClick={()=>setOpen(!open)}>التحكم بإظهار وإخفاء القوائم</button>{open&&<div>{hideable.map(name=><label key={name} style={{display:'flex',gap:12,padding:12}}><input type="checkbox" checked={!hidden.includes(name)} onChange={e=>{if(!toggleMenu(name,!e.target.checked))setError('تعذر حفظ إعدادات العرض.');}}/><span>{name}</span></label>)}<p>إعداد عرض على هذا الجهاز؛ إخفاء القائمة لا يحذف بياناتها.</p></div>}{error&&<p role="alert">{error}</p>}</section>;}
